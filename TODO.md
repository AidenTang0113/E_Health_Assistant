## 通用
- 每次循环后清屏
- 有时需要按两次回车确认，或选择选项后再回车确认
- 操作日志什么都没有

## LLM 配置
- 测试连接时为避免用户长时间等待，显示log信息（不用红色），测试结果不要只返回含义不明的raw信息
- 测试连接时连接未成功就不要切换mock后显示连接成功，误导用户已经连接成功
- 模型名称对三方和本地分别存储，现在切换模式后模型名不变，导致使用三方时出错

## 个人查看
-- HR 无个人查看，加入查看，给HR加入姓名更改